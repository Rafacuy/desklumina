/* Settings panel theme
 * 
 * Overrides lumina.rasi for Settings UI.
 */
@import "lumina.rasi"

* {
    bg-alt: @surface-alt;
}

element selected.normal {
    background-color: @selection-bg;
}

element selected.normal element-text {
    border:       0 0 0 2px;
    border-color: @selection-bg;
    padding:      2px 0 2px 8px;
    border-radius: 0;
}
